According to ISO C11, it violates a constraint to redefine the members of the same struct, union, or enum. In practice this is a compiler error that prevents the application from being built. The correct solution is to delete one of the tag definitions, incorporate both into a single tag, or rename one so that it represents a unique type.
