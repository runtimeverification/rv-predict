#!/usr/bin/env bash
ulimit -s `ulimit -H -s`
if [ -z "$RV_OPTS" ];
  then export RV_OPTS="-Xms64m -Xmx1024m -Xss32m"
fi

java $RV_OPTS -ea -cp "$(dirname "$0")/../lib/rv-predict.jar" com.runtimeverification.rvpredict.engine.main.Main "$@"
