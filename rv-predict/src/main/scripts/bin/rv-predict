#!/usr/bin/env bash
ulimit -s `ulimit -H -s`
if [ -z "$RV_OPTS" ];
  then export RV_OPTS="-Xms64m -Xmx1024m -Xss4m"
fi

if "$(dirname "$0")/checkJava"; then
  java $RV_OPTS -ea -jar "$(dirname "$0")/../rv-predict.jar" "$@"
else
  exit 1
fi
